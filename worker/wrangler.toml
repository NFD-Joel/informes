name = "informe-marks"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Run `npx wrangler kv namespace create MARKS` and paste the returned id below.
[[kv_namespaces]]
binding = "MARKS"
id = "4bd43f2525ae476f9eae4e958f68e0c8"

# Secret (do NOT put here) — set with: npx wrangler secret put WRITE_TOKEN
